---
description: Query and manage CDocs document metadata (status, state, tags, filters)
---

Load the `status` skill and execute it. The skill contains full instructions for querying and managing document metadata.

Arguments: $ARGUMENTS
