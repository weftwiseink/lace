#!/bin/sh
# No-op: the actual SSH daemon is installed by ghcr.io/devcontainers/features/sshd.
# This feature exists solely to declare lace port metadata (customizations.lace.ports).
echo "lace-sshd: metadata-only feature, nothing to install."
